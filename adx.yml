version: "3"

services:
  ckan:
    volumes:
      - ./adx_config.ini:/etc/ckan/production.ini
      - ../ckanext-unaids:/etc/ckanext-unaids
      - ./ckan-entrypoint-adx.sh:/ckan-entrypoint.sh
    command: ["ckan-paster","serve","--reload", "/etc/ckan/production.ini"]
